import '@web/dev-server-rollup';

// const replace = fromRollup(rollupReplace);

export default {
  plugins: [
    // replace({
    //   // setting "include" is important for performance
    //   include: ['src/logger.js'],
    //   'process.env.NODE_ENV': '"development"',
    // }),
  ],
};
